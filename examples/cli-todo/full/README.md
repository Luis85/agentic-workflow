---
title: cli-todo full archive
folder: examples/cli-todo/full
description: Unabridged historical mirror of the cli-todo worked example. Human reference only — agents should prefer the trimmed top-level artifacts in the parent folder.
entry_point: true
---

# cli-todo — full archive

Unabridged versions of every artifact produced for the `cli-todo` worked example. The trimmed top-level mirror at `../` is what agents should read; this folder is preserved so a human reader can see the complete shape of every artifact a real feature produces.

This folder intentionally has no `workflow-state.md` — the parent's `workflow-state.md` is the canonical workflow record. Files here are free-form reference content.

## Reading order

Same as the parent: `idea.md → research.md → requirements.md → design.md → spec.md → tasks.md → implementation-log.md → test-plan.md → test-report.md → review.md → traceability.md → release-notes.md → retrospective.md`.

## Project-local ADRs

[`adr/`](./adr/) holds ADR-CLI-NNNN entries scoped to this example. Same immutability rules as `docs/adr/`; numbering is independent.
